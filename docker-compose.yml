version: '3.3'
services:
  api:
    image: node:alpine
    ports:
      - '3000:3000'
    working_dir: /app
    volumes:
      - .:/app
    command: npm run start:dev
    environment:
      - NODE_ENV=development